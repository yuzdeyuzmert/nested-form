<p id="notice"><%= notice %></p>

<p>
  <b>Pnumber:</b>
  <%= @product.pnumber %>
</p>

<p>
  <b>Desc:</b>
  <%= @product.desc %>
</p>

<%= link_to "#{@product.desc}’s Parts", product_parts_path(@product) %>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>


<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pnumber %><br />
    <%= f.number_field :pnumber %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<% @parts.each do |part| %>
<%= form_for([@product,part]) do |f| %>
 
  <div class="field">
    <%= f.label :partnumber %><br />
    <%= f.number_field :partnumber %>
  </div>
  <div class="field">
    <%= f.label :partdesc %><br />
    <%= f.text_field :partdesc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>
